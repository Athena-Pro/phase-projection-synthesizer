import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.phaseprojsynth',
  appName: 'PhaseProjSynth',
  webDir: 'dist'
};

export default config;
